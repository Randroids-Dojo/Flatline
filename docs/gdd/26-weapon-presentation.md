# Weapon Presentation

**Status:** partial

Weapons are 2D foreground sprites, like classic shooters.

Each weapon needs:

- Idle frame
- Fire animation
- Cooldown animation
- Optional reload animation
- Pickup icon
- HUD icon

### Build log

- 2026-05-09: Filled the per-weapon frame set. `scripts/generate-weapon-sprites.mjs` now emits `{weapon}-cooldown.png` (gun in slight recoil with a fading ember halo, no muzzle spike), `{weapon}-pickup.png` (64x64 silhouette with a -30deg tilt and a per-weapon accent dot), `{weapon}-hud.png` (32x32 monochrome silhouette in the weapon's accent color), and `{weapon}-reload-{0..3}.png` (4-frame interpolated 0deg-to-90deg break-open tilt around the grip). Refactored the renderer to thread `width` / `height` per render so the same draw routine produces 384x256 game frames and 32x32 / 64x64 icons. Status stays `partial` because the new frames are committed to disk but `FlatlineGame.tsx` still only renders the idle / fire pair; F-018 tracks the wiring follow-up. Files: `scripts/generate-weapon-sprites.mjs`, `public/assets/weapons/{peashooter,boomstick,inkblaster}-{cooldown,pickup,hud,reload-0,reload-1,reload-2,reload-3}.png`. PR #124.
- 2026-05-03: Weapon recoil sprite kick added on every fire. The foreground sprite now translates downward and rotates slightly back during the firing window, then returns to rest. Per-weapon kick magnitude lives in pure helper `src/game/weaponRecoil.ts` (peashooter 6 px / -1.2 deg / 140 ms, inkblaster 10 px / -2.2 deg / 180 ms, boomstick 18 px / -3.5 deg / 240 ms). The animation is keyed by a fire counter so consecutive shots retrigger cleanly. Files: `src/game/weaponRecoil.ts`, `src/game/weaponRecoil.test.ts`, `src/components/FlatlineGame.tsx`, `app/globals.css`. PR #51.
- 2026-05-03: Foreground muzzle flash overlay added on each fire as an HUD-layer radial-gradient burst (peashooter / boomstick warm gold, inkblaster teal). This stacks above the existing fire-frame swap so the gun reads as actually firing even before the bolt travels. Pure helper `src/game/muzzleFlash.ts`. Files: `src/components/FlatlineGame.tsx`, `app/globals.css`. PR #50.
- 2026-05-03: Split out of `GDD.md`. Pre-spiral implementation: weapon sprites under `public/assets/weapons/`, generated by `scripts/generate-weapon-sprites.mjs`. Per-weapon idle / fire / cooldown frame coverage has not been audited end-to-end; status `partial` until each weapon has the full frame set verified.
