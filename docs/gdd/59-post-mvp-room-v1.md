# Post-MVP: Room State V1

**Status:** partial

The room should change during a run without becoming a maze:

- Door states: closed, warning, open, cooling down.
- Lighting intensity tied to pressure.
- One moving cover element that cycles predictably.
- Breakable or reactive props for juice, not tactics at first.
- More visible landmarks on each wall.

The room must remain readable at high pressure. Add one state change at a time and smoke it in browser.

As of 2026-05-10 the door state machine, lighting-by-pressure, named landmarks, breakable props (two crates), and the one moving cover element have shipped. Partition / broken-wall destructibility and additional moving cover elements remain explicit out-of-scope for this requirement; if a future playtest signal calls for them they ship as their own slices.

### Build log

- 2026-05-11: Crate destruction grants score and spawns a score floater. Closes the "score floaters" deferral from the breakable-cover-v1 build log. New `CRATE_DESTRUCTION_SCORE = 25` constant in `src/game/breakableCover.ts` (sized as one quarter of the base enemy kill score so smashing a crate feels like a satisfying environment punch without competing with kills as a scoring strategy). In `damageBreakableAt`, the destruction branch now adds the score to `scoreRef`, calls `setScore` so the HUD pill updates, projects the crate mesh position to screen space (same projection path as the enemy-kill floater), and pushes a new `ScoreFloater` (tier `'base'`) onto `scoreFloaters` with the standard TTL cleanup window. The floater spawns 0.4 m above the crate so it does not overlap the destruction dust pop. Existing destruction visual / audio (`spawnEnemyDeathPop` + 110 Hz cue) is untouched. Files: `src/game/breakableCover.ts`, `src/game/breakableCover.test.ts`, `src/components/FlatlineGame.tsx`. PR #164. Status stays `partial` (additional breakable prop types, partition / broken-wall destructibility, hitstop-on-break, and pre-break impact dust remain).
- 2026-05-10: Moving cover element. Closes the last unbuilt REQ-059 beat ("one moving cover element that cycles predictably"). One steel slab sweeps east-west across z=4 between the altar and the north spawn doors, traveling between x=-3.5 and x=3.5 on an 8-second linear triangle wave (4s each direction, no easing so the player can time a crossing by counting beats). New pure helpers in `src/game/movingCover.ts`: `MOVING_COVER_PERIOD_MS = 8000`, `MOVING_COVER_Z = 4`, `MOVING_COVER_MIN_X = -3.5`, `MOVING_COVER_MAX_X = 3.5`, `MOVING_COVER_HALF_W = 0.5`, `MOVING_COVER_HALF_L = 0.2`, `MOVING_COVER_HEIGHT_M = 1.8`. `movingCoverPositionX(elapsedMs, periodMs, minX, maxX)` does the triangle-wave interpolation with safe handling of zero / negative period and negative elapsed time. `movingCoverRectAt(elapsedMs)` returns the corresponding CoverRect. The slab is solid (not breakable): a new seed entry is appended to `ARENA_COVER_RECTS` at index 8 holding the start-of-cycle position, and FlatlineGame's animate loop overwrites that entry in `coverRectsRef.current` every frame before the player / enemy clamps run. The mesh (a `BoxGeometry(1.0, 1.8, 0.4)` with a dark steel `MeshStandardMaterial`) tracks the same rect each frame. `damageBreakableAt` now decrements `movingCoverRectIndexRef.current` whenever a breakable splice happens below it so the mover's rect stays addressable. `startRun` resets the elapsed timer to 0 and pins the index back to 8. Files: `src/game/movingCover.ts`, `src/game/movingCover.test.ts`, `src/game/coverCollision.ts`, `src/components/FlatlineGame.tsx`. PR #157.
- 2026-05-10: Cover-aware projectiles. `tickSpitterProjectile` now takes a `coverRects` param defaulting to `ARENA_COVER_RECTS` so existing callers and tests stay green; the per-call `inflateCoverRects` helper replaces the module-level `INFLATED_COVER_RECTS` const so a destroyed breakable rect stops blocking spitter projectiles mid-flight. `tickInkProjectiles` now takes the same `coverRects` and returns `{ enemyHits, coverRectHits }`; the inkblaster's per-step segment test reuses `segmentBlockedByRects` exactly the way the hitscan path does, and any cover-rect hit is routed through `damageBreakableAt` via the same `collapsePelletCoverHits` dedup helper. Net effect: every player weapon respects pillars (no more inkblaster passing through pillar geometry) and every player weapon damages crates (hitscan + projectile parity); enemy spitter projectiles fall through where a player just broke a crate (no ghost cover). Files: `src/game/spitterProjectile.ts`, `src/game/spitterProjectile.test.ts`, `src/components/FlatlineGame.tsx`. PR #156.
- 2026-05-10: Breakable cover v1. Two crates (the symmetric pair flanking the south doors, rect indices 4 and 5 in `ARENA_COVER_RECTS`) now break when shot by player hitscan (peashooter + boomstick). Pillars, partition, and broken-wall stay solid intentionally; this slice proves the verb on one prop type before opening prop-type choices. Multi-hit HP, drops, score floaters, hitstop on break, pre-break impact dust, partition / broken-wall destructibility, cover-aware inkblaster projectile, and runtime cover updates for spitter projectiles are all explicit deferrals. `segmentBlockedByRects` extended to return `{x, z, rectIndex}` so damage can identify which rect was hit. `tickEnemy` extended with an optional `coverRects` param so AI movement also respects the runtime list (defaults to `ARENA_COVER_RECTS` so existing tests stay green). New pure helpers in `src/game/breakableCover.ts`: `BREAKABLE_CRATE_HP = 1`, `spliceRectAt`, and `renumberMapAfterSplice` (so destroying a breakable rect at index N drops the entry and shifts higher keys down by one). `src/components/FlatlineGame.tsx` adds `coverRectsRef` (mutable clone of the seed list), `breakableMeshesRef` and `breakableHpRef` (parallel maps keyed by current rect index), and a `damageBreakableAt` callback that hides the mesh, retints the existing `spawnEnemyDeathPop` ring as the destruction puff, plays a 110 Hz `playCue` thud, and updates all three structures atomically. Multi-pellet boomstick blasts collapse to one HP tick per rect per shot so a six-pellet spread doesn't waste five tiers' worth of HP on one crate. The hitscan path computes `min(18, coverDist)` as the enemy ray cap so a shot consumed by cover never also lands on an enemy behind it. `startRun` resets the runtime rects and remaps the breakables from a stored canonical mesh-ref array so each run starts with both crates intact. Files: `src/game/coverCollision.ts`, `src/game/coverCollision.test.ts`, `src/game/breakableCover.ts`, `src/game/breakableCover.test.ts`, `src/game/enemies.ts`, `src/components/FlatlineGame.tsx`. PR #155. Status stays `partial` (moving cover, additional breakable prop types, breakable spitter projectile blocking remain).
- 2026-05-04: Door state machine v1 shipped. Spawn doors now run a three-phase post-spawn life cycle (opening burst at peak opacity 0.78 / scaleY 0.96 / warm white, open hold at opacity 0.46 / scaleY 0.84 / amber, cooling fade back to opacity 0.08 / scaleY 0.58 / teal) plus a 220 Hz sawtooth audio cue at the spawn frame so the player can hear which door produced an enemy. New pure helpers `src/game/doorState.ts` (phase + visual style) and `src/game/doorCue.ts` (audio cue style) carry the per-phase tuning so future tweaks live in one place. `src/components/FlatlineGame.tsx` switches `doorSignalTimersRef` from countdown-to-zero to elapsed-since-spawn, refactors `applyDoorSignals` to read the visual from the helper, sets the timer to 0 instead of 950 on the spawn frame, and adds the new `playDoorOpenCue` runtime. Files: `src/game/doorState.ts`, `src/game/doorState.test.ts`, `src/game/doorCue.ts`, `src/game/doorCue.test.ts`, `src/components/FlatlineGame.tsx`. PR #TBD. Status stays `partial` (moving cover that cycles predictably and breakable props remain). The four named landmarks (north clock, east furnace, south curtain, west organ) shipped earlier in `feat/arena-landmarks-curtain-organ`.
- 2026-05-03: Split out of `GDD.md`. Lighting-by-pressure exists. Door state machine, moving cover, breakable props, and the four named landmarks remain follow-on work; status `partial`.
