# Audio

**Status:** partial

## Music

Adaptive loop.

Intensity layers:

1. Low tension
2. Combat
3. High pressure
4. Near death
5. Boss-like surge, later

## Sound effects

Required:

- Weapon fire
- Enemy hurt
- Enemy death
- Enemy attack windup
- Player damage
- Pickup
- Door spawn cue
- Hazard warning
- Combo increase
- Run end

## Audio readability

Audio should help replace color cues.

Examples:

- Spitter windup has a distinct whistle.
- Brute charge has a bassy inhale.
- Health pickup has a soft sparkle.
- Hazard has a countdown click.

### Build log

- 2026-05-09: Crossfire damage cues now route through the per-type hurt cue helper. Both crossfire damage paths (hazard-on-enemy at the hazard tick, spitter-projectile-on-enemy crossfire splash) previously fired a flat `playCue(180)` blip regardless of which enemy was hit. They now call `playEnemyHurtCue(enemyHurtCue(type))` when the victim survives the hit so the player hears which enemy got tagged. The death case is intentionally left unfired at these sites because the existing kill detection block handles `playEnemyDeathCue` on any fresh `state === 'dead'` transition; double-firing would compound on every crossfire kill. Tracks `lastBurnedType` alongside the existing `lastBurnedLabel` / `lastBurnedKilled` in the hazard-on-enemy loop. No new helper or tests; this slice replaces two `playCue(180)` call sites with `playEnemyHurtCue` plumbing. Files: `src/components/FlatlineGame.tsx`. Status stays `partial` (adaptive music intensity still v1). PR #148.
- 2026-05-09: Combo increase audio tick. Closes the "Combo increase" required-SFX line. The milestone cue (`comboMilestone.ts`) fires only at thresholds 5 / 10 / 20; intermediate kills landed silently so the player heard a chunky cue at 5, nothing at 6 / 7 / 8 / 9, then another at 10. New pure helper `src/game/comboIncreaseCue.ts` exposes `comboIncreaseCue(combo)` returning a frozen sine `{ frequency, waveform, durationMs, gain }` that rises in pitch with the streak (base 360 Hz, +24 Hz per combo step, capped at 720 Hz) so the build feels like ascent. Sine timbre keeps the tick distinct from the sawtooth milestone cues; gain (0.022) sits well below the milestone family (0.05 to 0.074) so the tick is felt as texture, not foreground. New predicate `shouldPlayComboIncreaseCue(prev, current, crossedMilestone)` returns false when combo did not grow, when combo dropped (streak reset), or when a milestone is taking the same kill, so the increase cue never doubles up with the milestone callout. Wired in the kill-update branch right after the milestone check. Files: `src/game/comboIncreaseCue.ts`, `src/game/comboIncreaseCue.test.ts`, `src/components/FlatlineGame.tsx`. Status stays `partial` (adaptive music intensity still v1; the crossfire damage paths still use a generic 180 Hz blip). PR #147.
- 2026-05-09: Run end audio sting. Closes the "Run end" required-SFX line. New pure helper `src/game/runEndCue.ts` exposes `runEndCue()` returning a frozen sawtooth three-tone descending sting (320 Hz / 240 ms / 0.05, 200 Hz / 240 ms / 0.045, 110 Hz / 360 ms / 0.04, total 840 ms) and `runEndCueTotalDurationMs(style)`. Each tone is quieter than the last so the sting decays away rather than holding flat; sawtooth timbre cuts through any residual layer noise mid-fade as the music and rage layers stop. The sting is fired once at the end of `finishRun` in `src/components/FlatlineGame.tsx`, after the music / rage / pickup-loop layers stop so the cue plays in clear silence. New local helper `playRunEndCue` chains three Web Audio oscillators back-to-back, advancing `context.currentTime` between tones and closing the context after the last tone settles. Skips on `isPractice` runs because practice ends with "Practice run ended." which is a soft return, not a death. Files: `src/game/runEndCue.ts`, `src/game/runEndCue.test.ts`, `src/components/FlatlineGame.tsx`. Status stays `partial` (combo increase SFX still unaudited; adaptive music intensity still v1; the crossfire damage paths still use a generic 180 Hz blip). PR #146.
- 2026-05-09: Per-type enemy hurt audio cue. Closes the "Enemy hurt" required-SFX line. New pure helper `src/game/enemyHurtCue.ts` exposes `enemyHurtCue(type)` returning per-enemy `{ frequency, waveform, durationMs, gain }` so a hit on each enemy reads as a distinct quick blip (skitter triangle 620 Hz / 60 ms / 0.018, grunt square 380 Hz / 80 ms / 0.022, brute sawtooth 220 Hz / 110 ms / 0.030, spitter triangle 480 Hz / 90 ms / 0.024). Mirrors the death-cue waveform per type so the timbre stays consistent inside an enemy's audio family; stays shorter and quieter than the corresponding death cue per type so a hit does not feel as final as a kill, and so multiple plinks on a brute do not drown combat audio. Replaces the previous generic `playCue(320)` blip in the hurt-but-alive branch of `damageEnemyById` so the player can track damage on multiple enemies in flight by ear. New local helper `playEnemyHurtCue` in `src/components/FlatlineGame.tsx` mirrors the existing flat-pitch `playWindupCue` envelope. Files: `src/game/enemyHurtCue.ts`, `src/game/enemyHurtCue.test.ts`, `src/components/FlatlineGame.tsx`. Status stays `partial` (combo increase / run end SFX still unaudited; adaptive music intensity still v1; the crossfire damage paths in the animate loop still use a generic 180 Hz blip and would benefit from per-type cues in their own slice). PR #145.
- 2026-05-09: Per-weapon fire audio cue. Closes the "Weapon fire" required-SFX line. New pure helper `src/game/weaponFireCue.ts` exposes `weaponFireCue(weapon)` returning per-weapon `{ frequencyStart, frequencyEnd, waveform, durationMs, gain }` so each shot reads as a falling pitch envelope distinct to the gun (peashooter triangle 720 to 480 Hz / 70 ms / 0.028 quick pluck, boomstick sawtooth 180 to 60 Hz / 220 ms / 0.05 shotgun thud, inkblaster square 540 to 240 Hz / 130 ms / 0.038 wet blip). Replaces the previous generic `playCue(weapon === 'boomstick' ? 120 : 180)` blip that left peashooter and inkblaster sounding identical. New local helper `playWeaponFireCue` in `src/components/FlatlineGame.tsx` mirrors the `playEnemyDeathCue` envelope (linear attack, exponential decay, exponential frequency ramp). Wired at the existing fire branch right after `setMuzzleFlash`. Files: `src/game/weaponFireCue.ts`, `src/game/weaponFireCue.test.ts`, `src/components/FlatlineGame.tsx`. Status stays `partial` (enemy hurt / combo increase / run end SFX still unaudited; adaptive music intensity still v1). PR #144.
- 2026-05-09: Enemy death audio cue. Closes the "Enemy death" required SFX line. New pure helper `src/game/enemyDeathCue.ts` exposes `enemyDeathCue(type)` returning per-enemy `{ frequencyStart, frequencyEnd, waveform, durationMs, gain }` so each death reads as a falling pitch envelope (skitter triangle 720 to 320 Hz / 110 ms / 0.024, grunt square 420 to 160 Hz / 160 ms / 0.030, brute sawtooth 220 to 70 Hz / 280 ms / 0.040, spitter triangle 540 to 180 Hz / 200 ms / 0.032). Different waveforms per type so simultaneous deaths stay distinguishable. Heavier enemies are louder and longer so a brute kill feels weightier than a skitter pop. New local helper `playEnemyDeathCue` in `src/components/FlatlineGame.tsx` mirrors the existing `playWindupCue` envelope (linear attack to peak, exponential decay) and adds an exponential frequency ramp from `frequencyStart` to `frequencyEnd`. Wired into the existing kill detection block alongside `spawnEnemyDeathPop`, so every fresh `state === 'dead'` transition fires both the visual ring and the audio cue. Files: `src/game/enemyDeathCue.ts`, `src/game/enemyDeathCue.test.ts`, `src/components/FlatlineGame.tsx`. Status stays `partial` (weapon fire / enemy hurt / combo increase / run end SFX still unaudited; adaptive music intensity still v1). PR #143.
- 2026-05-08: Wave lull cue (recovery complement to wave horn). New helper `lullStartedBetween(prevRunMs, currentRunMs)` in `src/game/encounterWave.ts` returns true exactly on the cycle wrap (peak end / lull start). New `playWaveLullCue(enabled)` in `src/components/FlatlineGame.tsx` plays a soft sine descending from 220 Hz to 130 Hz over 360 ms at gain 0.038, reading as "the peak is over, breathe" -- a distinct timbre (sine vs the horn's sawtooth) and a longer envelope so the recovery cue is unmistakable. Wired alongside the existing `peakStartedBetween` check; sets a "Wave eased" status line. Files: `src/game/encounterWave.ts`, `src/game/encounterWave.test.ts`, `src/components/FlatlineGame.tsx`. PR #112.
- 2026-05-08: Last-ammo warning cue. New pure helper `src/game/ammoWarning.ts` exposes `justHitLastAmmo(weapon, previous, current)` returning true exactly on the transition where a fire takes the weapon's ammo from greater than 1 to exactly 1. Peashooter (infinite ammo) always returns false; ammo refills (going from low to high) never fire because the helper only matches depletion transitions. New `playLastAmmoCue(enabled)` in `src/components/FlatlineGame.tsx` plays a square wave 420 Hz to 220 Hz over 180 ms at gain 0.04 so the cue reads as "running dry" rather than the brighter triangle milestone palette. Wired into the fire branch right after `spendWeaponAmmo`. Files: `src/game/ammoWarning.ts`, `src/game/ammoWarning.test.ts`, `src/components/FlatlineGame.tsx`. PR #108.
- 2026-05-08: Combo milestone cues. New pure helper `src/game/comboMilestone.ts` exposes `COMBO_MILESTONES = [5, 10, 20]` and `crossedComboMilestone(prev, current)`, mirroring the score milestone helper but tracking the kill streak. Returns null when combo decreases (streak reset after timeout) so a rebuild does not refire crossed thresholds until the streak passes them again. New `playComboMilestoneCue(milestone, enabled)` in `src/components/FlatlineGame.tsx` plays a sawtooth double-tap rise (440 / 540 / 640 Hz start, peaks at 1.4x for 260 / 330 / 400 ms, gain 0.05 / 0.062 / 0.074); the rougher sawtooth distinguishes the streak cue from the triangle score milestone cue. Wired into the kill-update branch alongside the score milestone check; a new `previousCombo` snapshot drives the comparison. Files: `src/game/comboMilestone.ts`, `src/game/comboMilestone.test.ts`, `src/components/FlatlineGame.tsx`. PR #106.
- 2026-05-08: Personal-best cue. New pure helper `src/game/personalBest.ts` exposes `justCrossedPersonalBest(previousScore, currentScore, previousBest)` returning true exactly on the kill-update transition that takes the run over the player's local best. Strict greater-than (tying does not fire). Returns false when there is no previous best so a player's first run never plays the cue (no record to beat). New `playPersonalBestCue(enabled)` in `src/components/FlatlineGame.tsx` plays a triangle ascending arpeggio (720 Hz to 1080 Hz to 1440 Hz over 540 ms at peak gain 0.085), pitched higher and held longer than the top score milestone tier so the moment reads as personal achievement rather than threshold crossing. Wired into the kill-update branch right after the milestone check; a new `previousBestAtRunStartRef` snapshots `bestLocalScore(leaderboard)` on `startRun` so the comparison stays stable through the run. Files: `src/game/personalBest.ts`, `src/game/personalBest.test.ts`, `src/components/FlatlineGame.tsx`. PR #105.
- 2026-05-08: Score milestone callouts. New pure helper `src/game/scoreMilestone.ts` exposes `SCORE_MILESTONES = [1000, 5000, 10000]` and `crossedScoreMilestone(prev, current)` returning the highest threshold crossed in the update (or null). New `playScoreMilestoneCue(milestone, enabled)` in `src/components/FlatlineGame.tsx` plays a triangle two-step rise (start 520 / 660 / 800 Hz by tier, peak at 1.5x for 320 / 380 / 440 ms, gain 0.05 / 0.062 / 0.074) so each tier is audibly bigger than the last. Wired into the kill-update branch immediately after the score state advances; only one cue fires per kill even when a single payout vaults two thresholds. Files: `src/game/scoreMilestone.ts`, `src/game/scoreMilestone.test.ts`, `src/components/FlatlineGame.tsx`. PR #103.
- 2026-05-08: Crossfire stagger audio cue. Makes the F-016 stagger window audible so the player can hear an infighting opening as it opens. New `playCrossfireStaggerCue` in `src/components/FlatlineGame.tsx` (triangle oscillator, 360 Hz to 150 Hz exponential slide over 220ms, gain 0.046) reads as "knocked off rhythm" rather than a chase or impact cue. Wired into the `enemyMeleeArcCrossfire` handler and the spitter projectile crossfire callback; both compare the victim's `crossfireStaggerMs` before and after `applyCrossfireRetarget` and fire the cue only on a 0 -> > 0 transition, so cascade no-ops and rolls under probability stay silent. Files: `src/components/FlatlineGame.tsx`. PR #101.
- 2026-05-06: Sustained rage pulse audio layer. F-011 (PR #69) shipped the one-shot grant swoop and the screen tint but deferred the sustained pulse layer; this slice ships it. New constants `src/game/ragePulse.ts` (`RAGE_PULSE_BASS_HZ = 80` square bass above the 60 Hz music sawtooth, `RAGE_PULSE_THROB_HZ = 4` faster than the music's 3.2 Hz throb, `RAGE_PULSE_GAIN = 0.05`). `src/components/FlatlineGame.tsx` adds `ragePulseLayerRef`, `startRagePulseLayer` / `stopRagePulseLayer` (mirroring the music layer's lifecycle), and per-frame `setTargetAtTime` ramps so the layer mutes when `settings.audio` toggles off mid-rage. Lifecycle: started on rage activation, stopped on expiry / `startRun` / `finishRun` / unmount. Files: `src/game/ragePulse.ts`, `src/game/ragePulse.test.ts`, `src/components/FlatlineGame.tsx`. PR #86.
- 2026-05-06: Combo break audio cue. The required-SFX list mentions a "Combo" cue family; the increase cue is still unaudited but the *break* path now has a distinct sting. New pure helper `src/game/comboBreakCue.ts` exposes `comboBreakCue()` (descending two-tone triangle, 620 Hz then 360 Hz over 80 + 110 ms, gain 0.05) and `comboJustBroke(prev, current)` keyed off `COMBO_BREAK_MIN_PRIOR = 2`. Local `playComboBreakCue` in `src/components/FlatlineGame.tsx` mirrors the existing dash-cue envelope. The animate loop reads `prevActiveComboRef`, calls the predicate against the existing `activeCombo` recompute, and fires the cue when a 2+ streak times out. Reset on `startRun`. Files: `src/game/comboBreakCue.ts`, `src/game/comboBreakCue.test.ts`, `src/components/FlatlineGame.tsx`. PR #84.
- 2026-05-06: Adaptive music intensity layer (F-015). The "Music: Adaptive loop" line is now partially satisfied by a single procedural sub-bass thrash layer whose gain rises with the encounter pressure ratio. New pure helper `src/game/musicIntensity.ts` exposes `musicIntensityGain(pressureRatio)` (silent below 0.5, linear ramp 0..1 across 0.5..1.0, holds at 1 above 1.0) and constants for the bass / throb / ramp tuning. `src/components/FlatlineGame.tsx` adds `musicLayerRef`, `startMusicLayer` (60 Hz sawtooth bass + 3.2 Hz sine throb LFO modulating an inner gain + master gain), `stopMusicLayer`, and a per-frame `setTargetAtTime` call that ramps `masterGain` smoothly to the helper's output. Lifecycle is tied to run start / finish / unmount. The remaining intensity layers (low tension / combat / high pressure / near death) are still a single layer in v1; staged stems can layer in a follow-up. Files: `src/game/musicIntensity.ts`, `src/game/musicIntensity.test.ts`, `src/components/FlatlineGame.tsx`. PR #71.
- 2026-05-04: Door spawn cue. The required-SFX line "Door spawn cue" is now satisfied. New pure helper `src/game/doorCue.ts` exposes `doorOpenCue()` returning `{ frequency: 220, waveform: 'sawtooth', durationMs: 180, gain: 0.034 }` so a door spawn reads as a low metallic thunk distinct from the existing sine pickup cues, square / triangle damage cues, and the windup palette. The 0.034 gain sits between the loudest skitter windup (0.022) and the loudest player damage cue (0.05) so the door spawn never dominates a damage frame but is still audible across the room. New local helper `playDoorOpenCue` in `src/components/FlatlineGame.tsx` runs an attack-then-exponential-decay envelope (mirroring the player damage cue path) and is wired into the spawn frame inside the existing director-spawn branch immediately after the door signal timer is reset. Files: `src/game/doorCue.ts`, `src/game/doorCue.test.ts`, `src/components/FlatlineGame.tsx`. PR #TBD. Status `partial` (adaptive music, pickup loop sound, weapon fire / enemy hurt / enemy death / combo increase / run end SFX still unaudited).
- 2026-05-04: Pickup collection sparkle. The required-SFX line "Pickup" plus the audio-readability example "Health pickup has a soft sparkle" are now satisfied. New pure helper `src/game/pickupCue.ts` exposes `pickupCue(kind)` returning `{ firstFrequency, secondFrequency, waveform, firstDurationMs, secondDurationMs, gain }` and `pickupCueTotalDurationMs(style)`. The supply pickup style is two ascending sine tones (880 Hz then 1320 Hz, 110 ms then 150 ms, gain 0.04) which sits between the loudest enemy-windup cue (0.038) and the loudest player-damage cue (0.05) so collection cuts through combat audio without overpowering it. New local helper `playPickupCue` in `src/components/FlatlineGame.tsx` chains two web-audio oscillators back-to-back with attack + exponential-decay envelopes. Wired into the supply-altar collection branch in place of the prior `playCue(520)` blip. Files: `src/game/pickupCue.ts`, `src/game/pickupCue.test.ts`, `src/components/FlatlineGame.tsx`. PR #TBD. Status `partial` (adaptive music, weapon fire / enemy hurt / enemy death / door spawn cue / combo increase / run end SFX still unaudited).
- 2026-05-04: Hazard countdown click. The required-SFX line "Hazard warning" plus the audio-readability example "Hazard has a countdown click" are now satisfied. New pure helper `src/game/hazardCountdown.ts` exposes `hazardCountdownCue(kind)` returning `{ frequency, finalFrequency, waveform, durationMs, gain, finalGain }` and `hazardCountdownTicksBetween(kind, prevRunMs, currentRunMs)` returning the tick events that crossed the window. Per-kind tuning: flame lane bright square click (440 / 660 Hz, 60 ms), ink pool dull triangle click (300 / 460 Hz, 70 ms), falling light high sine click (720 / 1040 Hz, 55 ms). The final tick of each warning phase plays at the higher pitch and louder gain so the player hears "tick... tick... tick... DING" right before activation. The cycle constants now live in `hazardCycleConfigs` exported from `src/game/hazards.ts` so the countdown helper and `hazardStatesForRunMs` cannot drift apart. New local helper `playHazardCountdownCue` in `src/components/FlatlineGame.tsx` plays each tick with an attack-then-exponential-decay envelope. Wired into the animate loop next to `applyHazardMeshes` via a new `prevHazardRunMsRef` so the consumer detects edges across frames without storing per-hazard state. Files: `src/game/hazardCountdown.ts`, `src/game/hazardCountdown.test.ts`, `src/game/hazards.ts`, `src/components/FlatlineGame.tsx`. PR #TBD. Status `partial` (adaptive music, pickup loop sound, weapon fire / enemy hurt / enemy death / pickup / door spawn cue / combo increase / run end SFX still unaudited).
- 2026-05-04: Player damage audio sting. The required-SFX line "Player damage" is now satisfied with a distinct cue per source. New pure helper `src/game/playerDamageCue.ts` returns `{ frequency, waveform, durationMs, gain }` for `enemy` (200 Hz square, 180 ms, gain 0.045) and `hazard` (260 Hz triangle, 220 ms, gain 0.05). New local helper `playPlayerDamageCue` in `src/components/FlatlineGame.tsx` runs an attack-then-exponential-decay envelope. Wired into the enemy-melee-hit branch (replaces the previous silent damage event) and the hazard-tick branch (replaces the prior generic `playCue(70)`). Files: `src/game/playerDamageCue.ts`, `src/game/playerDamageCue.test.ts`, `src/components/FlatlineGame.tsx`. PR #TBD. Status `partial` (adaptive music, hazard countdown click, pickup loop sound, weapon fire / enemy hurt / enemy death / pickup / door spawn cue / combo increase / run end SFX still unaudited).
- 2026-05-04: Distinct per-enemy-type windup audio cues. New pure helper `src/game/enemyWindupCue.ts` returns `{ frequency, waveform, durationMs, gain }` per enemy type (skitter: 880 Hz sine 110 ms, grunt: 340 Hz square 150 ms, brute: 150 Hz sawtooth 280 ms). The `enemyAttackStarted` event in `src/game/enemies.ts` now carries `enemyType` so the FlatlineGame consumer can pick the right cue without re-deriving it. New local helper `playWindupCue` in `src/components/FlatlineGame.tsx` runs an attack-then-exponential-decay envelope so the cue does not click on stop. Files: `src/game/enemyWindupCue.ts`, `src/game/enemyWindupCue.test.ts`, `src/game/enemies.ts`, `src/game/enemies.test.ts`, `src/components/FlatlineGame.tsx`. PR #TBD. Status `partial` (adaptive music, hazard countdown click, pickup loop sound, full required-SFX list still unaudited).
- 2026-05-03: Split out of `GDD.md`. Pre-spiral implementation: generated audio cues are wired into `src/components/FlatlineGame.tsx`. Adaptive music layers, the full required-SFX list, and the audio-readability cues have not been individually audited. Status `partial`.
